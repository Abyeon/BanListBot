module.exports = {
    name: 'rep',
    description: 'Returns the reputation of the user',
    execute(client, channel, tags, message, args) {
        client.say(channel, "U h o h s t i n k y");
    }
}